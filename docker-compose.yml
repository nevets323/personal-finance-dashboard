services:
  finance-dashboard:
    build: .
    container_name: finance-dashboard
    ports:
      - "8743:8743"
    volumes:
      - finance-data:/data
    restart: unless-stopped

volumes:
  finance-data:
